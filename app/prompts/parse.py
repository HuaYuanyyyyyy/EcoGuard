PARSE_PROMPT = """结合对话历史分析用户当前输入，完成三件事：
1. 指代消解：如果当前输入中有"那XX呢"、"它"、"这个"等指代或省略，根据对话历史补全成完整的意思
2. 判断意图
3. 如果是合规检测，提取污染物数据

只输出JSON，不要输出其他任何内容，不要加代码块标记，字符串值内部不能包含双引号。

对话历史：
{history}

用户当前输入：{user_input}

输出格式：
{{
  "intent": "compliance或chat或irrelevant",
  "items": [
    {{"name": "污染物名称", "measured_value": "实测值"}}
  ]
}}

intent说明：
- compliance：用户输入了污染物数据（或指代了历史中提到的数据），需要合规检测
- chat：用户在询问问题，比如怎么使用、环保知识等
- irrelevant：与环保合规完全无关

注意：
- 如果intent不是compliance，items返回空数组[]
- 实测值可以来自当前输入，也可以是当前输入指代的历史内容中明确给出的数值
- 只提取有明确实测值的污染物；如果用户追问某污染物但当前输入和历史中都没有它的实测值，intent应为chat
"""
